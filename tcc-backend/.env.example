DB_URL=jdbc:postgresql://localhost:5432/tcc
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL_MODE=disable

# Render/Supabase production example:
# DB_URL=jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:6543/postgres?sslmode=require
# DB_SSL_MODE=require

# Generate a strong key (base64) and set here.
# Example command (PowerShell):
# [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }))
JWT_SECRET=replace_with_base64_secret_min_32_bytes
JWT_EXPIRATION_MS=2592000000

# Local defaults already include localhost and Vite ports.
# For Vercel, prefer the exact frontend URL; the default also accepts https://*.vercel.app.
CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:3000,http://localhost:5173,http://localhost:4173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://127.0.0.1:4173,https://*.vercel.app
